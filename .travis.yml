language: node_js

node_js:
  - "0.10"
  - "0.11"

matrix:
    allow_failures:
        - node_js: "0.11"

branches:
    except:
        - gh-pages

after_script:
    - if [[ `node --version` == *v0.10* ]]; then cat ./gh-pages/coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js; fi
